export const getPlayerInfo = (state) => {
    return state.player.playerInfo;
}

export const getWallet = (state) => {
    return state.player.wallet;
}

export const getMask = (state) => {
    return state.player.mask;
}

export const getHead = (state) => {
    return state.player.head;
}

export const getBody = (state) => {
    return state.player.Body;
}

export const getHand = (state) => {
    return state.player.hand;
}